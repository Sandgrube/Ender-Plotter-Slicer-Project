# Machine Profiles

This folder contains reference JSON copies of machine profiles.

The application currently ships with built-in profiles in `ender_plotter_slicer/profiles.py`. These JSON files are included for documentation and easy manual reuse.
